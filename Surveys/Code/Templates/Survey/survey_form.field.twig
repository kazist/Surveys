{# empty Twig template #}

{% import "form.macro.twig" as forms %}

{% set item = getContext('item') %}
{% set path_set = addTwigPath( "applications.Survey.Components.Templates.survey") %}

{% include "survey.new.form.twig" ignore missing with {'survey_id': item.id, 'hide_on_new': false } %}

{{ forms.input('id', 'hidden', item.id, null, 'survey_id') }}

